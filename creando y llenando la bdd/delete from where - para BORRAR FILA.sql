-- Delete from ... where 
-- para borrar filas 

-- inserto un dato que voy a borrar
--INSERT INTO ocupaciones (tipo_ocupacion, descripcion)
--VALUES ('Botones', 'Realizar recados');

--verifico si esta dato nuevo en la tabla
-- select * from ocupaciones

-- se borra dato nuevo en la tabla
-- DELETE FROM ocupaciones
-- WHERE id_ocupacion=3

-- verifico que dato este eliminado, ya no este en la tabla
select * from ocupaciones